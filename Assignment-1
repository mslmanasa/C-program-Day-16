#include <stdio.h>

int main() {
    int n, i;

    // Take input from user
    printf("Enter a number: ");
    scanf("%d", &n);

    // Print multiplication table
    for (i = 1; i <= 10; i++) {
        printf("%d x %d = %d\n", n, i, n*i);
    }

    return 0;
}
